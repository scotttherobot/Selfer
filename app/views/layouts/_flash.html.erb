<% if flash.count != 0 %>
<div class="flash-message">
   <% flash.each do |name, msg| %>
      <% if msg.is_a?(String) %>
         <h2><%= content_tag :p, msg, :id => "flash_#{name}" %></h2>
      <% end %>
   <% end %>
</div>
<% end %>
