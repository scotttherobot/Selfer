<%= form_for @post, :html => { :multipart => true } do |f| %>
   <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
   </p>
   <p>
    <%= f.label :image %><br>
    <%= f.file_field :image %>
   </p>
   <%= f.submit %>
<% end %>
